import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF0052FF);
  static const Color secondary = Color(0xFF00C853);
  static const Color tertiary = Color(0xFF7C4DFF);
  static const Color neutral = Color(0xFF73739E);
  /// Soft background for account / profile flows.
  static const Color surfaceMuted = Color(0xFFF5F6FA);
  static const Color cardBorder = Color(0xFFE8EAF2);
  /// Auth / form vibrant blue (login reference).
  static const Color formBlue = Color(0xFF0066FF);
  /// Filled inputs (login reference).
  static const Color inputFill = Color(0xFFF0F2F9);
}
